# Drupal normally puts a .htaccess alongside exported configuration, to protect it from
# prying eyes, but that's not relevant for a recipe.
/config/.htaccess

# Junk that macOS creates automatically.
.DS_Store

# Local Drupal.org issue preflight artifacts from agent triage.
.drupal-contribute-fix/

# Local peer-agent review packets and transcripts.
.peer/
